import { PrismaClientExceptionFilterFilter } from './prisma-client-exception-filter.filter';

describe('PrismaClientExceptionFilterFilter', () => {
  it('should be defined', () => {
    expect(new PrismaClientExceptionFilterFilter()).toBeDefined();
  });
});
